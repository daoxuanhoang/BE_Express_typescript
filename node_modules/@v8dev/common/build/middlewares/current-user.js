"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.currentUser = void 0;
var jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
var currentUser = function (req, res, next) {
    var _a, _b, _c;
    var token = ((_a = req.session) === null || _a === void 0 ? void 0 : _a.jwt) || ((_b = req.cookies) === null || _b === void 0 ? void 0 : _b.jwt);
    if (req.headers.authorization) {
        var bearer = (_c = req.headers.authorization) === null || _c === void 0 ? void 0 : _c.split(" ");
        token = bearer[1];
    }
    if (!token) {
        return next();
    }
    try {
        var payload = jsonwebtoken_1.default.verify(token, process.env.JWT_KEY);
        req.user = __assign(__assign({}, payload), { id: "".concat(payload.id) });
    }
    catch (err) {
        console.log(err);
    }
    next();
};
exports.currentUser = currentUser;

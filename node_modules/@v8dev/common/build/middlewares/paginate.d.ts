import { Request, Response, NextFunction } from "express";
interface Pagination {
    page: number;
    perPage: number;
    skip: number;
}
declare global {
    namespace Express {
        interface Request {
            paginate: Pagination;
        }
    }
}
export declare const paginate: (req: Request, res: Response, next: NextFunction) => Response<any, Record<string, any>> | undefined;
export {};
